let boolean aux;
let number aux; //Hola esto es un comentario bien formado
function number suma (number a) 
{    let number b;
    alert ("Sumar");
    input(b);
    return a + b;
}

function foo(){
	aux = true;
	let number prueba;
	let boolean booleano;
	booleano = true;
	prueba = suma(booleano);
	if((boolean & aux) number + 5;
}

